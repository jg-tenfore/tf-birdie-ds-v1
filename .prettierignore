# Build output
dist
storybook-static

# Vendor content — product catalogue metadata and reference screenshots are
# supplied as-is and are not ours to reformat.
store
references

# Generated — rewritten by `npm run generate:catalog`, so formatting it by hand
# would be undone on the next run.
src/data/store-catalog.ts

package-lock.json

# Prose is authored, not generated. Prettier rewrites `*emphasis*` to
# `_emphasis_` and reflows tables across every doc — pure churn on text that was
# written deliberately, and it buries real changes in a formatting diff.
# Code below `src/` is a different matter and is formatted.
*.md
*.mdx
